<h1>Question № <%= @question.test.questions.index(@question) + 1 %> for test <%= @question.test.title %></h1>

<p><%= @question.body %></p>
  <span>
    <%= link_to "Delete", question_path, method: :delete, data: {confirm: "Are you sure?"} %>
  </span>
</div>
